name: sexual_health_assignment
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  # Firebase
  firebase_core: ^1.2.0
  firebase_analytics: ^8.1.0
  firebase_auth: ^2.0.0
  cloud_firestore: ^2.3.0
  firebase_storage: ^9.0.0
  # Fonts
  google_fonts: ^2.1.0
  # State management
  provider: ^5.0.0
  # Bottom bar
  animated_bottom_navigation_bar: ^0.3.2
  # Spinner
  flutter_spinkit: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
